{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "echo 'MLB ingest cron disabled — enable after historical backfill (scripts/mlb_backfill.py) runs and real-data checks pass; then set startCommand to: python scripts/mlb_ingest_daily.py'",
    "restartPolicyType": "NEVER"
  }
}
