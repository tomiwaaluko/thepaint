{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "echo 'NBA offseason — prediction cron disabled until 2025-26 season'",
    "restartPolicyType": "NEVER"
  }
}
